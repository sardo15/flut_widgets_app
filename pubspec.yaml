name: widgets_app
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.0.0-339.0.dev <4.0.0"

dependencies:
  animate_do: ^3.0.2
  flutter:
    sdk: flutter
  go_router: ^6.5.7
  material: any

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
